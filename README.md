# essay_devel
1. clone repo
  
  $ git clone PATH

2. install pyenv

  https://github.com/yyuu/pyenv#installation
  
  installation section. all setup except zsh settings
  
  $ pyenv install 2.7.9
  $ pyenv rehash
  
2. install nvm
 
  https://github.com/creationix/nvm

  installation section.
  
  $ nvm install 0.10.38
  
3. change python env

  $ cd essay_devel
  $ nvm use && nvm current
  $ pyenv version
  $ source venv/bin/activate
  $ cd er.mohw
  $ long long command
  
